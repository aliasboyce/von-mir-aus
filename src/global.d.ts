/** Injected at build time via vite.config.ts's `define` — a fresh
 * timestamp string unique to each build, used by updateCheck.ts to
 * notice when a newer deploy is available. */
declare const __BUILD_ID__: string;
